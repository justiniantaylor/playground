security:
  sessions: ALWAYS
spring:
  session:
    store-type: redis
zuul:
  routes:
    resource:
      url: http://localhost:9000
      sensitive-headers:
    web:
      url: http://localhost:4200
      sensitive-headers:  
      
logging:
  level:
    root: WARN      